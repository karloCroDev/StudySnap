'use client';

// External packages
import * as React from 'react';

// Components
import { SearchableHeader } from '@/components/ui/SearchableHeader';

export default function Discover() {
  return <SearchableHeader title="Discover section" />;
}
